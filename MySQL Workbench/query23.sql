-- create table customers (
-- id int auto_increment primary key,
-- name varchar(255),
-- city varchar(255),
-- country varchar(255),
-- postalCode varchar(255));

-- insert into customers (name, city, country, postalCode)
-- values ("Rayan Verma", "Patna", "India", "201301"),
-- ("Rohan Borse", "Pune", "India", "402345");

-- Insert into customers (name, city, country, postalCode)
-- Select name, city, address, pin from student;

-- Select * from customers limit 10 offset 10;


